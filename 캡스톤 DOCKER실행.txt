캡스톤 DOCKER실행
soho010526@jsh-capstone1:~/Capstone1_server$  에서

sudo docker run --gpus all -d \
  --name capstone_flask \
  -p 8000:8000 \
  -p 5000:5000 \
  -v "$(pwd)":/app \
  --restart unless-stopped \
  capstone_flask


로그 키기
sudo docker logs -f capstone_flask

서버 재시작
sudo docker restart capstone_flask