캡스톤 DOCKER실행
sudo docker run --gpus all -d \
  --name capstone_flask \
  -p 8000:8000 \
  -p 5000:5000 \
  --restart unless-stopped \
  capstone_flask


로그 키기
sudo docker logs -f capstone_flask
